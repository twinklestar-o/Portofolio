---
import Header from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;

const startCareer = 2022;
const experience = new Date().getFullYear() - startCareer;
const metaDescription = "Third-year Computer Science student with hands-on experience in team and personal projects, specializing in web and mobile development."
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={metaDescription} />
		<meta name="viewport" content="width=device-width" />
		<base href="/Portofolio/" />
		<link rel="icon" href={`${import.meta.env.BASE_URL}img/favicon.png`} type="image/png">

		<link rel="stylesheet" href={`${import.meta.env.BASE_URL}css/bootstrap.css`} />
    <link rel="stylesheet" href={`${import.meta.env.BASE_URL}css/font-awesome.min.css`} />
    <link rel="stylesheet" href={`${import.meta.env.BASE_URL}css/style.css`} />
		
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<Header />

		<slot />

		<Footer />
	</body>
	<script src={`${import.meta.env.BASE_URL}js/jquery-3.2.1.min.js`}></script>
	<script src={`${import.meta.env.BASE_URL}js/popper.js`}></script>
	<script src={`${import.meta.env.BASE_URL}js/bootstrap.min.js`}></script>
	<script src={`${import.meta.env.BASE_URL}js/index.js?v=0.2`}></script>
	<script src={`${import.meta.env.BASE_URL}js/theme.js`}></script>
	<script is:inline src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

</html>
